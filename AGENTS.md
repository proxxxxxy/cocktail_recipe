# AGENTS.md — このプロジェクトで作業するAIへの引き継ぎ

ANTIGRAVITY Cocktail Index の開発ノート。**複数のAIが並行して触る前提**で書いています。
人間向けの概要は [README.md](README.md)、こちらは**作業手順・不変条件・過去の失敗**が主題です。

最終更新: 2026-09-19 / 作りやすい定番カクテル5種を追加

---

## 0. 30秒で把握する

| | |
| --- | --- |
| 何 | 日本語のカクテル・リファレンス。**159種**（IBA公認57種・ノンアル15種） |
| 構成 | `index.html` + `style.css` + `app.js` の3枚。**ビルド工程なし** |
| 公開 | GitHub Pages → <https://proxxxxxy.github.io/> （`main` にpushで即公開） |
| 実機 | **iOSのChrome**。モバイルWebKitが本番環境 |
| 規模 | app.js 約7,200行 / style.css 約3,000行 / index.html 約320行 |
| 自己点検 | `tools/selfcheck.js` — **79項目**（MIDORIの出典・検索・棚・分類も検証） |

```bash
python -m http.server 4173     # これだけで動く
```

---

## 1. 壊してはいけない3つ（最重要）

**すべて「静かに壊れる」種類のバグです。** 例外なくテストが見張っているので、必ず自己点検を回してください。

### 1-1. `SHELF_VOCABULARY` は末尾追加のみ

`app.js` の材料ID配列。**この並び順が、これまでに発行された全メニューリンクの意味そのもの**です。
棚の内容を1材料=1ビットでURLに詰めているため、途中に挿入・並べ替え・削除をすると、
**他人のLINEのトーク履歴に残っているリンクが、全部違う棚を指します。**

```js
const SHELF_VOCABULARY = [
  'gin', 'vodka', /* …61個… */
  // 追加はここ（末尾）だけ。上の行には絶対に触らない
];
```

自己点検が、41材料時代の7文字コードが今も同じ41本にデコードされることを実測で確認しています。

### 1-2. レシピキーの割り材はアルファベット順

キーは `ベース+割り材+割り材…`。割り材は**必ずソート順**。
ビルド画面もルーティングもキーをその順で組み立て直すため、順序が違うと**誰からも到達できないレシピ**になります。

```js
"gin+lemon+soda+sugar"   // OK
"gin+soda+lemon+sugar"   // 到達不能（エラーも出ない）
```

### 1-3. 名前で結合しているリストがある

`SERVE_ORDER`（食前/〆）、`SEASONS`（春夏秋冬）、`GLASS_BY_NAME`、`SOURCE_NOTES`、
`VERIFIED_AGAINST` — 全部**カクテルの日本語名**でデータベースと結合しています。
名前を変えたら、これら全部を追随させること。綴りが1文字違うと黙って外れます。

---

## 2. 作業フロー

```bash
# 1. app.js を編集したら
node tools/gen-recipes.js        # recipes.md を再生成（recipes.mdは生成物。直接編集しない）

# 2. app.js か style.css を触ったら
node tools/stamp-version.js      # index.html の ?v= を中身のハッシュで打ち直す

# 3. ブラウザで自己点検（下記）→ 79/79 を確認

# 4. コミット & push
git add -A && git commit && git push origin main
```

`--check` でCIのように検証もできます。

```bash
node tools/gen-recipes.js --check     # recipes.md が古ければ exit 1
node tools/stamp-version.js --check   # ?v= が古ければ exit 1
```

### 自己点検の回し方

ページを開いた状態で、コンソール（またはJS実行）に貼る:

```js
const s = document.createElement('script');
s.src = '/tools/selfcheck.js?t=' + Date.now();
s.onload = () => window.__selfcheck().then(r => console.log(r.summary, r.failures));
document.head.appendChild(s);
```

**79項目。** 上記3つの不変条件、全モードが**実際に画面に見えているか**、カウントと表示枚数の一致、
ルーティングの往復、メニューコードの後方互換、159杯すべてのサムネイル描画と度数表示、日本語文の体裁、
グラス表示と手順文の整合、重複ID、横スクロールの有無まで見ます。

---

## 3. 過去に実際にやらかした罠

**同じ穴に落ちないでください。全部このプロジェクトで実際に起きたことです。**

### 罠1: `querySelectorAll` の件数は「見えている」証明にならない

CSSの一括置換をミスして**「名前から」タブを画面から丸ごと消したまま公開**しました。
`display:none` の中でもノードは数えられるので、カード枚数を数えるチェックは全部素通り。

> 表示を確認するときは `offsetParent !== null && offsetHeight > 0`。件数ではなく可視性。

### 罠2: CSSセレクタの機械的な一括置換

`.simulator-layout[data-mode="dictionary"]` を置換したら、
`X .child { display:none }` が `X, Y .child { … }` になり、**子に効くはずの指定が親に落ちました**（6箇所）。
罠1と合わさって公開まで到達。

> セレクタの置換後は、必ず前後2行を目で見る。

### 罠3: `.hidden` と同詳細度の `!important` は後勝ち

印刷用CSSに `.result-section { display:block !important }` を書いたら、
`.hidden { display:none !important }` に**ソース順で勝って**、隠してあるパネルが印刷されました。

> `:not(.hidden)` を付ける。詳細度が同じなら後ろが勝つ。

### 罠4: アニメーション途中の値を読んでバグと誤認

度数表示が「26%のはずが10%」に見えたので「前の酒の値が残るバグだ」と判断して対策を入れました。
**誤診**で、カウントアップ演出の途中を読んでいただけ。しかも入れた対策は
「初回フレームが遅いと数字が逆走する」という**本物の不具合**を生むものでした。取り消し済み。

> 時間依存の値を読むテストは書かない。幾何や計算値で検証する。

### 罠5: Pythonで書き戻すとCRLFになる

`io.open(p,'w',encoding='utf-8')` は `\n` を `\r\n` に変換します。
差分が数千行に膨れ上がりました。

> **必ず `newline=''` を付ける。** コミット前に `d.count(b'\r\n')` が0か確認。

### 罠6: `?v=` の更新忘れ

スマホで見たとき、**古いキャッシュと「直っていない」は見分けがつきません**。
1回忘れて、動いているコードを疑って時間を溶かしました。→ `stamp-version.js` で自動化済み。

### 罠7: エントリ内検索のつもりで隣のエントリを読む

スクリプトで「このレシピに `isIBA` があるか」を前方1400文字で調べたら、
**次のレシピのフラグ**を拾って「既にある」と誤判定しました。

> レシピ単位の走査は、次のキー `\n  "xxx+` までで範囲を区切る。

---

## 4. データの足し方

### カクテルを1杯足す

`app.js` の `cocktailDatabase` に追加。必須フィールドは自己点検が検証します。

```js
"gin+lemon+soda+sugar": {          // 割り材はソート順（不変条件1-2）
  name: "ジン・フィズ",             // 日本語名＝各種リストの結合キー
  enName: "Gin Fizz",              // URLスラッグの元
  abv: 14,                          // モクテルは必ず 0
  taste: ["甘酸っぱい", "爽快", "きめ細かい泡"],   // 2〜4個
  description: "…",                 // 30文字以上
  color: "rgba(…)",
  hasBubbles: true,
  garnish: "lemon",                 // lime/lemon/orange/cherry/mint/olive/
                                    // coffee_bean/nutmeg または null（飾らないという判断）
  ice: "cube",                      // cube / crushed / none
  isIBA: true,                      // IBA公認102種に載っている場合のみ
  summer: true,                     // 任意
  ingredients: [{ name: "…", amount: "45 ml" }],
  method: ["…。"],                  // 各行は句点で終わる
},
```

足したら `SERVE_ORDER` / `SEASONS` / 必要なら `GLASS_BY_NAME` にも名前を追加。

### 材料を1つ足す

1. `mixerDefinitions` に定義（ベースにもするなら `baseTints` / `baseNameMap` / `baseIconMap` にも）
2. **`SHELF_VOCABULARY` の末尾**に追加 ← 不変条件1-1
3. アルコールを含むなら `ALCOHOLIC_MIXERS` に追加（モクテル判定に効く）
4. 新ベースなら `index.html` にボタン、`tools/gen-recipes.js` の `SECTIONS` にも追加
5. 新ベースなら `style.css` の `#view-build .base-btn[data-base="材料ID"]` に `--ingredient-rgb` を設定。未設定だと共通の金色になる。通常時と選択時を確認する

### カクテル/モクテルの判定は書かない

`isMocktailKey()` が**ベースと割り材から導出**します。フラグの付け忘れで分類が狂わない代わりに、
ベースの登録（`NON_ALCOHOLIC_BASES`）が正しいことが前提です。

---

## 5. 出典（未完・引き継ぎたい作業）

レシピは**もともと全部AIの知識から書かれたもの**で、一次資料に当たっていませんでした。
そこで出典表示の仕組みを入れ、照合を始めたところです。

| | |
| --- | --- |
| 照合済み | **63種**（IBA公認57種＋MIDORI公式4種・サントリー公式2種）。各公式レシピを確認 |
| 未照合 | **96種** → レシピ画面に「出典未照合」と明示中 |

照合の結果、**IBA53杯のうち20杯が間違っていて修正**しました（マイタイのシロップ欠落、
エスプレッソ・マティーニのシロップ欠落、ビーズ・ニーズのオレンジ欠落など）。
**AIの記憶は当てにならない、という実証データです。** 未照合の96種も同じ精度だと考えてください。

### 続きのやり方

```js
// app.js
const SOURCES = {
  iba: { label: 'IBA 公式レシピ', org: '国際バーテンダー協会 (IBA)', url: '…' },
  // ここに追加。例: suntory / asahi / kirin
};
const VERIFIED_AGAINST = {
  iba: ['ネグローニ', …],   // 実際にページを見て確認した銘柄名だけを入れる
};
const SOURCE_NOTES = {
  // 出典と意図的に違える場合に理由を書く。黙って直さない・黙って名乗らない
  'サウスサイド': 'IBAの公式仕様はレモン30mlですが、本書は…',
};
```

MIDORIを使う5種は各レシピの `sourceUrl` で個別の公式ページへリンクします。公式のpartsは一杯分のmlへ換算し、度数の前提や適量の目安を `SOURCE_NOTES` に記載しています。`midori`、`sour_mix` に続いて `vanilla_liqueur` と `ginger_beer` を材料IDの末尾に追加済みです。サワー削除後も共有リンクの互換性のため `sour_mix` のIDは残します。ユーザーの希望は「MIDORI＋割り材」の羅列ではなく、固有名のある実在カクテルです。

フッターの「全159種のうち63種を照合済み」は**実行時に数えている**ので、
名前を足せば自動で増えます。良く見せる嘘が書けない作りです。

**未照合96種の内訳**: 日本発・日本で定着したもの（カシス・オレンジ、レゲエ・パンチ、
ソル・クバーノ、チャイナ・ブルー、マリブ系5杯、パルフェタムール系2杯…）とノンアル15種。
IBAに載らないので、**サントリー / アサヒ / キリン**あたりの大手レシピDBが照合先の候補です。

> 照合していないものを `VERIFIED_AGAINST` に入れないでください。
> この仕組みの価値は「見ていないものを見たと言わない」ことにあります。

---

カンパリは既存の材料IDをそのままベースにも使用しています。スプモーニ、アメリカーノ、ガリバルディ、フレンチ・コネクション、フレンチ・マティーニは公式ページを個別確認済みです。

## 6. 主要な機能（把握しておくとよい）

- **名前からの使用材料フィルター** — レシピキーの全材料が対象。ベースだけの比較に戻さないこと。共有メニューでも割り材として使用されるお酒のボタンを出す。テキスト検索にはベース名と割り材名の両方を含める（MIDORI／ミドリ等）。

- **カクテル/モクテル切替** — 全モード横断。モクテル側はアクセントが真鍮→緑に変わる（CSS変数3つで全体が切り替わる）
- **ゲスト用メニュー** `#/menu/<code>` — 棚をURLに埋め込み、友人に渡す。タブもビルド画面も出ない「UIが減ったモード」。章立て（はじめ/食事とともに/〆）＋「おまかせで一杯」
- **手持ちから** — 棚のチェックから作れる一覧。「最初に揃える5本」は総当たり探索（結果はキャッシュ）
- **印刷CSS** — 紙用に白地の品書きへ切り替わる
- **出典表示** — 各レシピ末尾＋フッター

---

## 7. コミット規約

- メッセージは**英語**。1行目は要約、本文で**なぜそうしたか**と**何を確認したか**を書く
- 末尾に自己点検の結果（`Self-check: 79/79`）
- `main` に直接コミット＆push（Pagesがそのまま公開するため）
- 節目には git タグ（`backup-2` が直近の復帰ポイント）

---

## 8. まだ誰も確認していないこと

**見た目です。**

このプロジェクトのAI作業は、フレームを合成しないブラウザ環境で行われてきました。
スクリーンショットが撮れないため、座標・算出スタイル・canvasのピクセル値でしか検証していません。
**配色が美しいか、3列のカードが窮屈でないか、余白が気持ちいいかは、人間が実機で見た分だけが確認済み**です。

デザインを変えたら、必ず実機確認を依頼してください。「確認しました」と言わないこと。
